const Provider = require('./provider');
const Tournament = require('./tournament');
const Match = require('./match');
const Team = require('./team-stats');
const Participant = require('./participant-stats');
const Game = require('./game');

module.exports = {
  Provider,
  Tournament,
  Match,
  Team,
  Participant,
  Game,
};
